#!/usr/bin/env bash

# kc-logout - gives a quick dmenu command for chosing to 
# shutdown, logout, or restart
#
# Depends on elogind

choice=$( echo -e "Shutdown\nRestart\nLogout" | dmenu -i -h $KC_BAR_HEIGHT -p "You're leaving? What would you like to do:" )


case "$choice" in
    "Shutdown")
        sudo /bin/shutdown -P now
        ;;
    "Restart")
        loginctl reboot
        ;;
    "Logout")
        loginctl | egrep -v "root|SESSION|listed" | awk '{print $1}' | xargs loginctl terminate-session
        ;;
    *)
        ;; # do nothing
esac

